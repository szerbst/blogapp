<%= simple_form_for([@project, @project.posts.build]) do |f| %>
	<%= f.input :title, label: "Post Title" %>
	<%= f.input :content, label: "Post Description" %>
	<%= f.input :tag_list, label: "Tags" %>		
	<%= f.button :submit %>
	<%= link_to "Cancel", project_path(@project) %>
<% end %>