<%= simple_form_for([@imageable, @imageable.images.build]) do |f| %>
	<%= f.input :caption %>
	<%= f.input :picture, as: :file %>
	<%= f.button :submit %>
	<%= link_to "Cancel", [@imageable, :images] %>
<% end %>